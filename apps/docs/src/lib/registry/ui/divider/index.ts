import Divider, { type DividerProps } from './exports/Divider.svelte';
import { divider, type DividerVariantProps } from './styles';

export {
    // components
    Divider as Root,
    Divider,
    type DividerProps,
    // styles
    divider,
    type DividerVariantProps
};
